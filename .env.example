# Google Earth Engine (optional - only needed for satellite data fetching)
GEE_PROJECT_ID=your-gee-project-id-here
# GEE_SERVICE_ACCOUNT=your-service-account@project.iam.gserviceaccount.com

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=False

# Paths (auto-detected if not set - only override if needed)
# MODEL_DIR=./models
# DATA_DIR=./data
